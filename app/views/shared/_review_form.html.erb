<%= form_for [@book, @review] do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_field :content, placeholder: "Your review...", class: "form-control" %>
  </div>

  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
