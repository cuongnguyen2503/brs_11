<% provide(:title, 'Edit book') %>

<div class = "col-md-8 col-md-offset-2">
  <%= form_for @book do |f| %>
    <%= render 'book_fields', f: f %>

    <%= f.submit "Submit", class: "btn btn-primary" %>
  <% end %>
</div>